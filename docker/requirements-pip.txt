# Core
# scipy, requests, urllib3, pillow, huggingface_hub are provided by the vllm:26.03 base
# image — pip constraints file in the Dockerfile pins them. Cython, matplotlib, pytest,
# pandas, toml were provided by pytorch:26.03 but NOT by vllm:26.03, so we install them
# explicitly below. fvcore, fairscale, torchmetrics, shapely are imported by tao-pytorch
# (whose tests run against this base image), so we keep them; apache-libcloud and openmim
# remain removed (not imported in nvidia_tao_ds, tests, tao-core, or tao-pytorch).
# transformers/pynvml kept because tao-core imports them.
accelerate==1.13.0
--extra-index-url=https://pypi.nvidia.com
cudf-cu13==26.4.0
cuml-cu13==26.4.0
h5py==3.16.0
lightning_fabric==2.6.1
mpi4py==4.1.1
opencv-python-headless==4.13.0.92
open-clip-torch[training]==2.30.0
# pycocotools is not shipped by vllm:26.03 base — we install 2.0.11 from PyPI here.
pycocotools==2.0.11
pyfacer==0.0.5
pytorch_lightning==2.6.1
scikit-image==0.26.0
timm==1.0.26
# vllm:26.03 ships transformers 4.57.5 + huggingface_hub 0.36.2. Match the base pin so
# the constraints check passes; do not bump to 5.x (would conflict with vllm's HF hub).
transformers==4.57.5
wandb==0.26.1

# Provided by pytorch:26.03 base but missing from vllm:26.03 base — added back here.
Cython==3.2.4
matplotlib==3.10.9
nvidia-dali-cuda130==2.1.0
nvidia-nvjpeg2k-cu13==0.10.0.49
pandas==2.3.3
# pyarrow is required by pandas for parquet I/O (pd.read_parquet/to_parquet) used in
# nvidia_tao_ds.rcca.gap_analysis.scripts.vcn_aoi. Aligned with pytorch:26.03 base.
pyarrow==23.0.1
pytest==8.1.1  # held to global PIP_CONSTRAINT in vllm:26.03 base (/etc/pip/constraint.txt)
toml==0.10.2

# Imported by tao-pytorch (tao-pytorch is pip-developed into this image during CI tests).
fvcore==0.1.5.post20221221
fairscale==0.4.13
torchmetrics==1.9.0
shapely==2.1.2

# Imported by nvidia_tao_ds.data_analytics.scripts.analyze (and tao-pytorch).
seaborn==0.13.2

# LLM clients (video_reasoning_annotation pipeline)
google-genai>=1.0.0
openai>=2.0  # 1.x pins anyio<4; base ships anyio 4.12.1

# API
apispec==6.10.0
apispec_webframeworks==1.2.0
flask==3.1.3
flask_limiter==3.13
marshmallow==3.26.1
marshmallow_enum==1.5.1
# tao-core utilities pulled in transitively by nvidia_tao_ds.core.logging
# (see tao-core/requirements.txt). Kubernetes / microservice runtime deps
# are intentionally omitted here per upgrade plan.
ruamel.yaml==0.19.1
marshmallow-oneofschema==3.2.0
orjson==3.11.8
pymongo==4.17.0
# PyJWT not pinned here: vllm:26.03 base ships 2.12.1 and the constraints file pins it.
validators==0.35.0
timeloop==1.0.2
# kubernetes is imported unconditionally in tao-core/microservices/utils/core_utils.py.
# Upstream PyPI client (latest) — the custom-built v23.6.0 of the original Dockerfile is gone.
kubernetes==35.0.0
# ngcbase (from ngcsdk) is imported unconditionally in tao-core/microservices/utils/ngc_utils.py.
ngcsdk==3.151.0
# leptonai is imported in tao-core/microservices/handlers/lepton_handler.py (transitively).
# All published versions pin anyio<=4.9.0, conflicting with base anyio 4.12.1.
# Installed --no-deps in the Dockerfile so the strict pin doesn't apply at resolution time.

# Utilities
hydra-core==1.3.2
imagesize==1.4.1
# spatialai_data_utils is installed --no-deps in the Dockerfile because its strict
# tqdm==4.67.1 pin would force a downgrade of base tqdm 4.67.3.
# nvidia-eff is installed --no-deps --ignore-requires-python in the Dockerfile because
# (a) its PyPI metadata mis-declares requires_python==3.8.* but cp312 wheels exist, and
# (b) its declared deps include legacy pins (pyarmor==8.5.8, isort<5, black==22.3.0).
# Required by tao-pytorch's eff.core/eff.codec checkpoint encryption imports.
omegaconf==2.3.0
prettytable==3.17.0
pynvml==13.0.1  # 12.x pins nvidia-ml-py<13 and would downgrade base 13.595.45
ujson==5.12.0

# Dev
pydocstyle==6.3.0
flake8==7.3.0
pyflakes==3.4.0
pylint==4.0.5  # 3.x pins isort<7 conflicting with base isort 8.0.1
pytest-order==1.4.0
pyinstaller==6.20.0
